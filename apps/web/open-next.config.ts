import { defineCloudflareConfig } from "@opennextjs/cloudflare"

// Full-offload parachute config (see cloudflare/RUNBOOK.md Part B).
// Not used by the Vercel build — only by `opennextjs-cloudflare build`.
export default defineCloudflareConfig({})
